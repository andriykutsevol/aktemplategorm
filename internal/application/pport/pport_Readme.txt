- Defines primary ports (interfaces) like UserService

Dependency Inversion Principle:
- By placing the port in the application layer, you invert the dependency
  such that the driving adapter depends on the port/interface, not the other way around.
-   